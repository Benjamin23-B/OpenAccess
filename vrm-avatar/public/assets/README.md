# VRM Assets Directory

Please place your character VRM model here and name it:
`avatar.vrm`

Vite will serve it at the URL `/assets/avatar.vrm`, which is automatically fetched by the React Three Fiber renderer.
